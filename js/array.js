//un array es una lista de elementos. orden de 0 al ultimo elemento. arranco siempre desde cero 
//para el index o cualquier pagina es importante el orden que se llaman los archivos .js ya que si uno define un elemento tiene que ser llamado antes de que otro js lo necesite.
//.length dice el largo del Array, por ejemplo array [1,2,3,4] nos diria que el valor es 4